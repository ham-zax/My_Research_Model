# Experiment 001 implementation status

Updated 2026-09-25. Branch: `feat/e001-milestone1`; starting revision `02436fb`. The checkout now contains implementation commit `3e6cd34` plus the follow-up fixes documented here. Existing work was retained; no experiment freeze tag has been created. This ledger supersedes earlier statements that Tardis samples could not be downloaded.

## Current result

**Original E001 execution result:** the [runner](../scripts/run_e001_experiment.py) selected 29 hash-verified sealed BTC segments and six diagnostic decisions. Its latest [real readiness report](../data/derived/e001_readiness_004/report.md) is `BLOCKED_TIMING`: zero primary-eligible rows and no original E001 real model fit or score. The report independently records missing independent UTC evidence, unavailable public WebSocket role-delay bounds, and missing feature economics/history. The [bounded official-source review](../docs/e001_websocket_evidence_review.md) explains the source-delay finding; the [feature matrix](../docs/e001_feature_feasibility.md) records the selected-row coverage. Waiting for another short capture cannot resolve the missing source contract.

**Separate exploratory BTC result:** after the owner selected a free observational route, the [versioned hourly protocol](../experiments/e001_observational_exploratory_protocol.yaml) scored 126 paired held-out decisions from seven of 19 first-of-month sample days. The [result](../docs/e001_exploratory_btc_result.md) found baseline Brier 0.13207137 and MFSM Brier 0.13190346, an improvement of only +0.00016791. Both fitted models were worse than a post-score historical-rate diagnostic at 0.12661880, and leaving out June reverses the sign of the MFSM gain. This does **not** demonstrate predictive or trading edge and does not resolve original E001 eligibility. The [full prediction receipt](e001_observational_btc_result.json) and [manifest](e001_observational_btc_manifest.json) are tracked; the raw BTC episode ledger remains in ignored `data/derived/e001_observational_002/`. No key or ETH access was used.

**Capacity measurement result:** the owner-approved [25 bps capacity audit](e001_capacity_audit_report.md) replayed all 22 predefined sharp-selloff crossings from hash-verified Bybit perpetual L2 and trade files. The eight December crossings lack the required 30 minute prehistory. All 14 February/March crossings have history but fail full bid-band coverage: the worst per-second coverage within each event history is only 0.718–1.198 bps, and the largest coverage seen is 5.278 bps. Zero crossings and zero independent episodes can measure the existing proxy, so no rank relationship or trading-edge conclusion was computed. No model was fitted and no threshold was changed. The [tracked JSON summary](e001_capacity_audit_results.json) points to the full result and manifest in ignored `data/derived/e001_capacity_audit_003/`.

**Separate near-touch capacity result:** the predeclared [0.5 bps experiment](../docs/e001_shallow_capacity_result.md) expanded spot-only event discovery to 42 fixed first-of-month days before reading capacity outcomes. It found 90 crossings, 20 nominal episodes and 12 conservative history-qualified episodes. After acquiring and hash-verifying Bybit L2/trades for 16 event days, only five crossings and two independent episodes passed every measurement gate. Fifty-nine crossings had a zero-depth second inside the 0.5 bps midpoint band, 17 had stale/invalid history and nine lacked complete replenishment history. No correlation or model was computed. Both surviving independent rows had no 25 bps breach while gross sell flow exceeded the proxy. This narrower definition is also too fragile and does not validate original E001. See the [tracked receipt](e001_shallow_capacity_result.json).

**Fresh fixed-level liquidity study is now prespecified, but not scored.** The [E001-LIQ-OBS-1 protocol](../experiments/e001_liquidity_observational_protocol.yaml) was frozen while forward run `9c4ee7b3301742aabf31377f5d2c894c` was still acquiring and before event labels or model scores were inspected. It replaces the literal capacity-limit claim with observable top-50 bid/ask depth relative to 30-minute history, spread, imbalance, 30-second buy/sell flow and 15-second persistent bid replenishment. B4 and MFSM receive the same eight shared measurements; MFSM adds only two fixed interactions between sell pressure and low bid depth / weak replenishment. The minimum real evaluation remains 130 eligible episodes with the existing chronological 50/20/20/20 walk-forward blocks and two full outer test blocks. The current 20,673-second forward run is a qualification seed only: with a 7,200-second lockout it can contain at most three independent episodes, so it cannot satisfy that evaluation gate. Previous 42 shallow-study dates and the prior hourly exploratory sample are excluded from validation. Real fitting remains disabled until the capture is sealed/hash-verified, feed/epoch/gap quality passes and the sufficiency gate is met. No execution-cost protocol exists, so this study cannot establish trading profitability.

**Synthetic software result:** the explicit [fixture run](../data/derived/e001_fixture_005/report.md) is `PIPELINE_VERIFIED_SYNTHETIC`. Its receipt grid yielded four accepted episodes with downside-first, recovery-first, unavailable and neither-by-horizon labels; a separate invented model fixture produced 40 paired out-of-sample software predictions with a synthetic Brier difference of +0.0241011382. That number has no market interpretation. The original E001 real BTC score remains null. The full suite now passes **160 tests** with the pinned `eval` extra; ETH remains sealed. The [candidate evaluation protocol](../experiments/e001_btc_evaluation_protocol.yaml) is software-only because the real supported feature contract is unresolved.

**Receipt-time revision candidate prepared, still unqualified.** The owner selected preparation of a receipt-time policy. `E001-receipt-time-candidate-1` now has a [versioned contract](../docs/e001_receipt_time_revision.md), separate feature schema, operational quality states and a [fixed-input gate result](e001_receipt_policy_gate_results.json). On the original bad-clock BTC smoke its 3.327-second source-clock lead triggers quarantine. The repaired-clock and short probe smokes remain in warm-up; all three longer-prefix decisions are in warm-up after an observed event-loop delay. Independent UTC attestation and a WebSocket-role source-delay bound are unavailable, so **zero candidate rows are primary eligible**. No receipt-time events, labels or model were produced; the strict default and ETH seal are unchanged. The stopped development capture has not been restarted.

**Timing Delivery A is implemented as diagnostics; no primary timing policy is selected.** The [comparison](e001_timing_policy_comparison.md) and [machine-readable manifest](e001_timing_policy_comparison.json) reproduce the strict 0.331859-ms failure and audit four fixed captures plus a new 15-second probe smoke. The recorder now stores bounded public HTTP clock evidence; the probe smoke sealed 1,968 records and replay accepted one Binance and one Bybit probe. The HTTP clock relation has not been established for WebSocket `ts`, `T`, or `E`. Receipt-time quote opportunities exist even in the original 3.3-second error capture, so these diagnostic counts cannot qualify the period. The strict primary replay, features and labels are unchanged; capture recovery and model fitting remain gated. See the [timing contract](../docs/e001_timing_contract.md).

The [receipt-time feature comparison](e001_receipt_feature_comparison.json) tests actual feature rows on identical sealed inputs. It produced 80 raw diagnostic measurements where strict replay produced zero in both the original clock-error smoke and the later recovery smoke. At clock-valid decisions, four to six flow/persistence fields changed value when measured on receipt time, confirming that this is a substantive feature-definition revision. Two decisions after the recurring discrepancy had 91 receipt-time raw measurements each, while full-band bid depth and its pre-trigger scale remained unavailable. Every real receipt row remains `primary_eligible: false`; the strict default behavior is preserved.

The owner-approved spot midpoint/five-second candidate has now been scanned across **19 isolated first-of-month days, March 2025 through September 2026**. All 38 quote files were acquired and verified; no date failed. Overall grid coverage is **99.98069%**. There are **22 observed crossings, 18 nominal lockout exclusions and four nominal labelled episodes** (one downside-first, two recovery-first, one neither). Only **two** have eligibility supported by the conservative missing-history audit; both are binary-negative outcomes on March 1, 2026. This cannot support chronological model training, calibration and evaluation **under the original shock-event rule**. No model was fitted on those shock events; the separate hourly exploratory study above uses a different question and label.

Current result: [monthly sample report](e001_monthly_sample_report.md), with [machine-readable evidence](e001_monthly_sample_results.json). The original three-day audit remains preserved in `artifacts/e001_spot_reference_audit.md` and `artifacts/e001_quote_audit_results.json`.

The next acquisition implementation is now available: [continuous BTC recorder](../docs/e001_continuous_capture.md). All three public connections were reached without credentials. The initial 60-second live check captured every requested topic, including a liquidation message; its [integrity audit](e001_capture_initial_smoke.json) is preserved. These checks are software/data-access evidence, not eligible model-training periods. A separate [receipt-ordered replay](../docs/e001_capture_replay.md) now reconstructs books and ticker state with explicit clock, sequence and depth-coverage gates. The full common feature panel remains unfinished.

The final implementation passed a separate 45-second live check: **5,209 WebSocket messages**, a Binance REST depth snapshot, all three connections, six transport heartbeat responses and no connection errors. Its [integrity audit](e001_capture_final_smoke.json) includes code hashes matching the final collector. The quiet liquidation channel emitted no event in this final interval; the initial check verified that channel with an observed event. Ticker payloads included OI, funding, mark and index fields.

The original forward run `54dccf2599ba4c6e9394cbc20a660dc8` was **stopped and sealed** after replay and public clock probes exposed approximately 3.3 seconds of local clock lag. Its [integrity audit](e001_capture_before_clock_repair.json) passed with 150,615 records and zero queued unwritten records. Historical timestamps remain unchanged and unqualified for the primary grid. Chrony ran with clock control disabled (`-x`); the owner repaired Windows host time.

Fresh verification reported synchronized Windows service status and [exchange offset bounds containing zero](e001_clock_probe_host_fixed.json). A new 60-second capture passed [integrity](e001_capture_clock_fixed_smoke.json) and [replay](e001_replay_clock_fixed_smoke_results.json): 7,195 WebSocket messages, 58 valid composite seconds out of 63 including startup/shutdown, and no clock-review or sequence-gap diagnostics. The 1,000-level perpetual book still lacked full bid-band coverage for six of 59 initialized seconds; incomplete full-band values remain missing.

Development capture restarted at **2026-09-24 01:06:01 UTC**, run `53d688bfe33349aaab40dc8e7463c802`, PID at launch `10220`. Planned stop is **2026-09-25 00:19:44 UTC**, within the original 00:19:49 deadline. The 16 GiB directory-wide raw cap includes the preserved interrupted run. Output: `data/raw/live_btc_development/`; log: `collector-clock-fixed.log`. The [new launch receipt](e001_forward_capture_clock_fixed_start.json) verified all three sources, a Binance snapshot, a current checkpoint and code hashes matching the successful fresh capture. Verify live status from the process, checkpoint and manifest before resuming work. The outage remains unknown history, and this is not a completed development dataset yet.

**Current operational status: stopped, not restarted.** At 05:31 UTC, continuation found no collector process and an available writer lock. The last durable checkpoint was **03:59:45 UTC**, with **1,468,361 records**; there was no terminal marker. The [interruption observation](e001_capture_interruption_observation.json) preserves the checkpoint and original manifest hash. The final 2,965,504-byte open tail was sealed as `.unclean` without changing its bytes; 119 clean segments remain eligible only for explicit prefix audits. The original manifest still says `running` because the process never finalized it; that historical field is not live status.

A fresh recovery smoke capture passed [storage integrity](e001_capture_recovery_smoke.json), but [replay](e001_replay_recovery_smoke_results.json) rejected timing: Bybit receipt-minus-source lag median **-2 ms**, minimum **-5 ms** (spot) / **-4 ms** (linear). Windows reported synchronized, last sync error zero and phase offset about 35.6 ms. This is residual cross-clock disagreement, not the earlier 3.3-second offset. The existing strict negative-lag gate remains unchanged; long capture was not restarted and no old timestamps were repaired.

The [live common-feature builder](../docs/e001_live_features.md) is implemented with 24 feature tests. It extracts supported raw measurements and shared normalized inputs at explicit decision seconds, preserving unsupported economics and insufficient history as null. The [smoke feature report](e001_features_clock_fixed_smoke_results.json) contains 67 available measurements; the [first 12-segment report](e001_features_forward_prefix_results.json) contains four clock-valid diagnostic rows. These are not research episodes or evidence of prediction. The longer capture exposed clock-invalid trade messages; extraction now records clock-invalid rows instead of aborting, with every numeric feature unavailable after the clock failure.

The [119-segment extended audit](e001_features_extended_prefix_results.json) completed successfully: one of three sampled decisions was clock-valid with 100 available raw measurements, and two were clock-invalid with all numeric features missing. All pre-trigger depth scales were unavailable. The [first recurring clock discrepancy](e001_first_recurring_clock_failure.json) occurred at 01:43:09 UTC and was only 0.331859 ms. This supports reviewing the strict timing assumption; it does not authorize silently modifying it.

A [post-warm-up decision audit](e001_features_pretrigger_audit_results.json) verified a complete 30-minute OI median of 59,225.673 BTC and a relative OI feature of +0.4369%, while its pre-trigger full-band depth scale remained missing. That row was clock-valid with 94 available raw measurements and valid bid-persistence. The feature pipeline can construct a causal denominator when the source history supports it; it does not fill a missing depth denominator with a partial-book estimate.

Candidate versions are `e001-v1.2`, `E001-raw-candidate-2`, `E001-features-candidate-2`, and `E001-label-v3`. The manifest remains pending with null resolved hashes and dates; ETH observations remain unopened. The next agent must not interpret the candidate version names as existing Git tags or a completed freeze.

## Implemented paths

- `data.py`: synthetic BTC Bybit V5 trade/liquidation/L2 adapter, receipt timestamps, duplicate checks, side/unit rules and gap markers.
- `events.py`, `labels.py`: one-second trigger crossings, two-hour lockout, 15-second decision delay and barrier labels with missingness reasons. Label availability cannot precede the grid time where an outcome becomes observable.
- `features.py`: fixture book reconstruction, depth, persistent additions, causal flow windows and shared B4/MFSM components. Full neutral feature coverage and matched models remain unfinished.
- `public_archive.py`, `cli.py`: BTC archive and Tardis CSV audits; fixture output; real model build remains gated.
- `tardis_spot.py`, `scripts/e001_spot_feasibility.py`: retained legacy last-trade/one-second feasibility probe, labelled with its original raw candidate version.
- `spot_quotes.py`, `scripts/e001_quote_audit.py`: current quote midpoint reference, exact microsecond availability, capture-order states, invalid-quote handling, age/continuity/spread/disagreement diagnostics, per-second provenance, and sample episode/label ledgers.
- `scripts/fetch_e001_tardis_sample.py`: BTC-only first-of-month downloads; `--profile spot-quotes` restricts acquisition to two spot quote files. `scripts/audit_e001_tardis_sample.py` records full-file coverage.
- `experiments/e001_quote_audit_protocol.md`: fixed data-quality sensitivity panel. The primary five-second candidate was approved before its episode extraction. No outcome or prediction score selected the age limit.
- `experiments/e001_monthly_scan_protocol.md`, `scripts/e001_monthly_scan.py`: fixed 19-date scan, source/code hash validation, per-day checkpoints and explicit failures.
- `sampling.py`, `scripts/e001_monthly_report.py`: conservative possible-lockout history audit, distinguishing nominal labels from history-qualified labels; tracked coverage, crossing and label evidence plus implementation handoff.
- `collect.py`, `capture_store.py`, `capture_health.py`: BTC-only public recorder with original payloads, application receipt clocks, fixed-duration manifests, bounded queues/storage, segment hashes, crash-tail preservation, reconnect/heartbeat and sequence diagnostics. Optional pinned `collect` dependency.
- `scripts/audit_e001_capture.py`: completed-run integrity audit, per-topic counts, ticker-field inventory and missing Binance snapshot diagnostics. Does not certify source completeness.
- `replay.py`, `scripts/replay_e001_capture.py`: hash-verified closed captures or frozen sealed prefixes; Binance snapshot/range reconciliation, separate Bybit depth books, ticker-delta provenance, conservative quote freshness, finite-band coverage and clock gating. Outputs deterministic one-second grids and reports; no model fit.
- `scripts/probe_e001_clock.py`: bounded public exchange-time probes with original responses and local-step checks; never changes clocks or historical timestamps.
- `timing.py`, `scripts/audit_e001_timing.py`: causal HTTP-domain evidence selection, explicit uncertainty helpers, timestamp-role lag audit and separate strict/receipt candidate diagnostics. Collector clock probes are stored without changing primary eligibility.
- `capture_features.py`, `scripts/build_e001_capture_features.py`: bounded flow/book/scalar histories, explicit decision-time features, full pre-trigger denominators, shared dimensionless representation and deterministic diagnostic artifacts. The optional receipt-time diagnostic and `receipt_v1` policy candidate use the same feature calculations while marking all rows ineligible. `receipt_v1` reports clock/feed/history quality states and unresolved source-delay evidence. `scripts/compare_e001_timing_features.py` verifies paired inputs and outputs. Pressure ratios use 30-second flows; replenishment uses 15 seconds. Full model inputs remain gated.
- `exploratory.py`, `scripts/run_e001_exploratory.py`: separate first-of-month BTC observational study with hash-verified source CSV, hourly receipt-time features and labels, preset calendar splits, matched calibrated learners, per-episode exclusions and a preserved paired prediction receipt. It does not qualify original E001 rows.
- `capacity_audit.py`, `scripts/audit_e001_capacity.py`: fixed-event Bybit L2 reconstruction and receipt-time trade audit for the 25 bps visible-capacity proxy. It verifies raw hashes, same-timestamp snapshot/delta cohorts, full bid-band coverage, persistent additions, pretrigger depth history, forward sell flow, breach/recovery outcomes and independent-episode sufficiency. The current archive fails the measurement gate before outcomes are scored.
- `scripts/discover_e001_shallow_capacity_events.py`, `scripts/finalize_e001_shallow_event_source.py`: spot-only fixed-calendar discovery and conservative lockout-history finalization for the separate 0.5 bps experiment. Discovery cannot calculate labels or capacity outcomes. The capacity audit accepts an explicit band while retaining 25 bps as its default.

## Installed data

- Original event-time-only Binance and Bybit BTC spot trade archives for 2023-10-01 remain in ignored `data/raw/2023-10-01/`.
- Tardis 2025-03-01 BTCUSDT: Binance and Bybit spot trades/quotes, Bybit perpetual trades, L2, liquidations, derivative ticker. March audits found 26,774,896 L2 rows, 1,418,703 perpetual trades, 2,092 liquidation rows and 158,149 derivative-ticker rows. All audited rows had local timestamps and none preceded exchange time.
- Tardis first-of-month samples 2025-04-01 through 2026-09-01: full BTC sample profiles now include Binance/Bybit spot trades and quotes plus Bybit perpetual trades, L2, liquidations and derivative ticker. Together with March 2025, these are 19 nonconsecutive days, not 19 months of continuous observations. The exploratory study uses only the two spot quote files, perpetual trades and derivative ticker from each day.
- Raw sources and SHA-256 values are recorded under `data/raw/tardis/DATE/manifest.json`; the tracked monthly results retain all 38 quote-file hashes. Direct access succeeded using a Mozilla-style User-Agent after initial 403/404 probes failed.

## Reproduce

```bash
uv sync --locked --extra test
python3 scripts/fetch_e001_tardis_sample.py --date 2025-03-01
python3 scripts/audit_e001_tardis_sample.py --date 2025-03-01
python3 scripts/fetch_e001_tardis_sample.py --date 2025-04-01 --profile spot-quotes
python3 scripts/fetch_e001_tardis_sample.py --date 2025-05-01 --profile spot-quotes
uv run --locked python scripts/e001_quote_audit.py
uv run --locked python scripts/e001_monthly_scan.py --download --workers 2
uv run --locked python scripts/e001_monthly_report.py
uv run --locked --extra collect --extra eval python scripts/run_e001_exploratory.py --output data/derived/e001_observational_REPRO
uv run --locked python scripts/audit_e001_capacity.py --output data/derived/e001_capacity_audit_REPRO
uv run --locked python scripts/discover_e001_shallow_capacity_events.py --download --workers 2 --output data/derived/e001_shallow_event_discovery_REPRO
uv run --locked --extra collect --extra eval --extra test pytest -q
```

Observed verification: prior baseline **114 passed**; current full suite **160 passed** using `uv run --locked --extra collect --extra eval --extra test pytest -q`. Nine focused capacity tests cover causal decision cutoffs, breach flow, recovery outcomes, missing history, epoch resets, explicit deep/shallow bands, bid-versus-ask coverage, Tardis snapshot/delta cohorts, receipt-time trade selection, history qualification and independent-event censoring. Earlier exploratory, live-feature, replay, capture, quote and history checks remain in the full suite. The shallow runner verified all 32 L2/trade source hashes for 16 event days and emitted a result/report manifest. `git diff --check` and `git diff --cached --check` passed.

## Remaining gates and next eligible work

Execution order, acceptance criteria and terminal result statuses are consolidated in [the next implementation plan](../docs/e001_next_implementation_plan.md). Runner, synthetic grid/dataset and matched evaluator software now exist. The plan does not activate a new primary timing policy.

1. Resolve the recurring millisecond clock disagreement under an explicit timing policy before restarting primary development acquisition. Host synchronization alone is insufficient proof. Any uncertainty allowance or change to availability rules needs a versioned design before use; do not quietly relax the current gate. Keep the interrupted 119-segment prefix and unclean tail distinct.
2. Replace the insufficient depth archive before retesting the existing capacity proxy. The implemented audit proves that the downloaded Bybit files cover at most 5.278 bps in the selected histories, below the fixed 25 bps requirement. The separately versioned 0.5 bps experiment also failed measurement sufficiency because the band becomes empty during spread widening. A suitable original-claim source must provide continuous historical BTCUSDT perpetual L2 at least 25 bps deep, receipt timestamps, recoverable snapshots/sequences, 30 minutes of pre-event history and enough independent selloff episodes. No paid access or account creation has been used.
3. Resolve liquidation notional without interpreting bankruptcy price as execution price; preserve the trade/liquidation contamination flag. Finalize the supported common feature list and candidate quality rules, then join independently eligible trigger/label ledgers to decision-time features. The 19-day quote scan still supplies only two history-qualified labels, both negative.
4. Finalize chronological BTC splits, model selection and calibration, then implement/fill the matched B4/MFSM evaluation from the approved plan. No synthetic or sparse-sample result can pass the BTC development gate.

The better long-term spot rule uses a synchronized reconstructed book with explicit source-health evidence. The approved five-second midpoint is a documented provisional rule for available quote archives. Preserve both facts when resuming. Do not expand to stocks, trading execution or ETH confirmation under the current data evidence.
